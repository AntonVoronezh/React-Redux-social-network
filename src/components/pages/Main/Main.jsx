import React from 'react';
import PropTypes from 'prop-types';

const Main = props => {
	return <React.Fragment>Main ggg</React.Fragment>;
};

export default Main;

Main.propTypes = {
	usernameText: PropTypes.string,
};
